import 'mocha';
import {expect} from 'chai';


describe('League test', async () => {
    // beforeEach( {

    // })


    it('testExample', async () => {
        expect(5).to.be.equal(5);
        expect(3).to.be.equal(3);
        
    })
});